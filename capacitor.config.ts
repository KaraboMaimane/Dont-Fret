import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.karabomaimane.dontfret',
  appName: 'Dont Fret',
  webDir: 'dist/dont-fret/browser'
};

export default config;
